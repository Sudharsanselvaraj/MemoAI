# Voice AI Agent — Dependencies

# Backend 
fastapi>=0.111.0
uvicorn>=0.30.1
python-multipart>=0.0.9

# STT — Groq (primary, fastest)
groq>=0.9.0

# LLM — Ollama HTTP client (no extra package needed, uses requests)
requests>=2.31.0

# Env management
python-dotenv>=1.0.0

# ── Optional: Local STT (HuggingFace Whisper) ─────────────────────────────────
# Uncomment if running local inference:
# transformers>=4.40.0
# torch>=2.0.0
# librosa>=0.10.0
# soundfile>=0.12.1

# ── Optional: OpenAI fallback ─────────────────────────────────────────────────
# openai>=1.30.0